"""
Tickets

`get_ticket(id)`  Gets a ticket with the ID specificed.
`get_tickets(user=None,date=None)` Gets tickets with specified parameters.
`update_ticket(id,chn)` Update a ticket with the ID specified. Channel should be the channel of the ticket.
`close_ticket(id)`  Close a ticket with the given ID.
`create_ticket(message)` Create a ticket based on the message given.

"""
